# 이 프로젝트에서 항상 지킬 것

## 개인정보 보호 (가장 중요)
- 화면 검증/스크린샷은 항상 "[TEST]" 접두사가 붙은 합성(가짜) 데이터로만 진행한다. 실제 쿨메신저 메시지, 실제 학생/교직원 이름, 실제 API 키 값이 화면에 떠 있는 상태로 스크린샷을 찍지 않는다.
- 전체 화면 캡처 대신 앱 창 단위로만 캡처한다.
- 실수로 실제 개인정보나 API 키가 찍힌 캡처가 생기면 즉시 파일을 삭제하고, 다음 보고에 그 사실을 반드시 알린다 — 숨기지 않는다.
- API 키를 "보기" 상태(평문)로 띄운 채로 캡처하지 않는다.

## 빌드
- 배포용 설치 파일을 만들 때는 build.ps1을 사용한다 (dist/build 폴더 삭제 → 실행 중 인스턴스 종료 → PyInstaller --clean → ISCC 순서가 하나의 스크립트로 원자적으로 묶여 있음). setup.iss를 단독으로 실행하지 않는다 — 낡은 PyInstaller 산출물을 그대로 재포장하게 된다.

## 진단 원칙
- "고쳤습니다"라고만 보고하지 않는다. 여러 단계로 이루어진 기능이 안 될 때는 각 단계(읽기 → 저장 → 표시 등)를 하나씩 직접 확인해서 정확히 어느 단계에서 문제가 생겼는지 짚어서 보고한다.

## 버전 규칙
- 기존 기능 수정(버그 수정 등): 패치 버전 0.0.1씩 증가
- 새 기능 추가: 마이너 버전 0.1.0씩 증가
